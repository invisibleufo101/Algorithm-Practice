#include <bits/stdc++.h>
using namespace std;
int n, arr[1000002];

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> n;
    for (int i=0; i<n; i++){
        cin >> arr[i];
    }


    return 0;
}