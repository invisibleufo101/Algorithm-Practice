#include <bits/stdc++.h>

using namespace std;

#define PI 3.14
#define loop(x,n) for (int x=0; x<n; x++)

int main() {
    int a[20];

    a[1] = 2;
    a[2] = 4;

    cout << a[1] << "\n";

    return 0; // <- End 
}

// If the code has changed, then we need to
// 1. g++ -std=c++14 -Wall test.cpp -o test.out
// 2. ./test.out
